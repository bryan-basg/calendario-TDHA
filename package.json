{
    "name": "calendario-tdha-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install --include=dev && chmod +x node_modules/.bin/vite && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
