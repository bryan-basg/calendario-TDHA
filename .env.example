# Configuración de Base de Datos
# Para desarrollo local, usa SQLite (por defecto)
# Para producción, usa PostgreSQL u otra base de datos
DATABASE_URL=sqlite:///./calendario.db
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Seguridad JWT
# ¡IMPORTANTE! Genera una clave segura para producción usando:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_clave_secreta_super_segura_y_aleatoria_cambialaprod
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS - Orígenes permitidos (separados por comas)
# Para desarrollo: http://localhost:5173,http://127.0.0.1:5173
# Para producción: https://tudominio.com
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# Entorno de ejecución (development o production)
ENVIRONMENT=development

# Configuración de HTTPS (solo para producción)
# ENABLE_HSTS=true

# Configuración de Push Notifications (opcional)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
